<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="org.impeng"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="4" />
    <application android:icon="@drawable/icon" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
        <activity android:name=".DiaryPro"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DiaryList"
                  android:label="@string/app_name">
        </activity>
        <activity android:name=".DiaryNew"
                  android:label="@string/app_name">
        </activity>
        <activity android:name=".DiaryReg"
                  android:label="@string/app_name">
        </activity>
        <activity android:name=".DiaryView"
                  android:label="@string/app_name">
        </activity>
        <activity android:name=".DiaryEdit"
                  android:label="@string/app_name">
        </activity>
        <activity android:name=".DiarySearch"
                  android:label="@string/app_name">
		</activity>                     
    </application>
</manifest>